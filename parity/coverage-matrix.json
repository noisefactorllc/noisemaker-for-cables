{
  "schemaVersion": 1,
  "title": "GAP-002 rendered parity coverage matrix at the existing checkpoint",
  "checkpoint": {
    "authority": {
      "kind": "vendored Noisemaker runtime pinned by vendor.lock.json",
      "package": "noisemaker",
      "runtime": "8eeb7b5a",
      "coreSha256": "092c3b776003bc1539bed91aa86f421f839b40b1aa8b81ea09a5ec5e6b7bd3c7",
      "coreFile": "vendor-cache/noisemaker-shaders-core.esm.js"
    },
    "frameSize": { "width": 64, "height": 48 },
    "channelCeiling": 0,
    "comparisonMethod": "independent-reference-adapter-float-readback",
    "comparisonDescription": "Every rendered effect runs twice through independent programs: once on the reference WebGL2Backend and once on the public CablesWebGL2Backend (Cables CGL) adapter, with identical deterministic inputs, audio/MIDI state, and external media texture. RGBA float readbacks of the presented internal texture are compared pixel-for-pixel with a zero mismatched-channel ceiling; the adapter's internal texture must also copy bit-exactly to the stable CGL output texture, and both readbacks must be finite. Per-effect GL resource accounting on both contexts must return to baseline (host resource check).",
    "compilerFixtureNote": "Compilation itself is not a parity claim: the independent authority comparison is rendered pixels, not graph equality."
  },
  "denominator": {
    "catalogEffects": 210,
    "renderedAndCompared": 209,
    "compileOnly": [
      {
        "id": "filter/octaveWarp",
        "classification": "headless-swiftshader-execution-pathology",
        "rendered": false,
        "reason": "Rendering this effect in headless SwiftShader exceeds the harness execution window; it is compiled and linked only, and this exclusion stays visible in the sweep result and in every coverage claim."
      }
    ],
    "sources": {
      "catalogPrograms": "vendor-cache/effects/manifest.json (210 effect IDs) with programs generated by parity/catalog-inputs.js; overrides in test/browser/harness/pipeline.js SPECIAL_CATALOG_DSL and EXTRA_CASES",
      "representativePrograms": "parity/programs.json (11 programs) plus the parameter/behavior EXTRA_CASES"
    },
    "nondeterministicOverlay": [
      {
        "id": "filter/fibers",
        "classification": "nondeterministic-canvas-overlay-generation"
      },
      {
        "id": "filter/scratches",
        "classification": "nondeterministic-canvas-overlay-generation"
      },
      {
        "id": "filter/strayHair",
        "classification": "nondeterministic-canvas-overlay-generation"
      }
    ],
    "nondeterministicOverlayNote": "These three effects generate their overlay through async 2D-canvas drawing (seeded but progress-driven). They remain rendered and compared on every sweep; their recorded mismatch counts are source-bound measurements. The zero-mismatch ceiling is asserted for every other rendered effect, and any mismatch outside this classified set fails the sweep."
  },
  "parameterMatrix": {
    "catalogSweep": "Each of the 210 effects renders its catalog-default program once against the independent reference at the checkpoint size with zero mismatched channels.",
    "representativeSuite": "26 source-bound frame comparisons (test/browser/pixel-parity.spec.js) exercise the parameter dimensions below at the same checkpoint and ceiling.",
    "dimensions": {
      "parameters": [
        "solid color/alpha variants (starter, named-subchain-and-outputs)",
        "noise seed, ridges, and displacement parameters (catalog default programs use seed: 1, ridges: true; landscape fixtures use seed: 4, ridges: true; octaveWarp and cubemap fixtures use seeds 1, 4, and 5)",
        "text font/size/color/matte parameters (text-bitmap)",
        "audio waveform/spectrum color, thickness, and gain (audio-waveform, audio-spectrum)",
        "midi grid color, gain, and speed (midi-grid)",
        "landscape filtering: voxel and isosurface define choices (landscape-voxel, landscape-isosurface)",
        "cubemap threshold and per-face basis matrices (six-cubemap-faces, volume-cubemap)"
      ],
      "statefulFrames": {
        "cases": ["feedback", "points-agents", "navier-stokes", "reaction-diffusion"],
        "captureFrames": [0, 1, 12],
        "note": "Stateful effects are compared at three frames so accumulated state, not just the first pass, is source-bound."
      },
      "media": {
        "case": "media@0",
        "note": "A deterministic 32x24 external RGBA8 texture is bound through findExternalTextureIds on both sides with identical pixels."
      },
      "sizes": {
        "checkpoint": "64x48 for every catalog and representative case",
        "beyondCheckpoint": "cubemap cases concatenate six face captures; native-host resize coverage is tracked separately under GAP-003"
      },
      "chains": ["starter", "named-subchain-and-outputs", "artistic-chain", "remap-ubo", "mesh", "points-agents"],
      "hostResourceChecks": "Every catalog sweep iteration snapshots GL object counts (buffers, framebuffers, programs, queries, renderbuffers, samplers, shaders, textures, transform feedbacks, vertex arrays) on both the adapter and reference contexts before and after the effect; any delta fails the sweep as a visible resource-leak failure."
    }
  },
  "visibility": {
    "skips": "The single compile-only exclusion is asserted by ID in test/browser/full-catalog.spec.js; any other unrendered effect fails the suite.",
    "refusalsAndTimeouts": "Thrown errors from compile, initialize, render, and disposal become failure records with case context (error.task9 diagnostics); context loss breaks the sweep with an explicit failure.",
    "failures": "Comparison failures carry mismatchedChannels, max/mean channel error, and the first divergent pixels."
  },
  "outOfScope": {
    "nativeEditorHost": "Standalone editor qualification remains under GAP-003.",
    "releaseAcceptance": "Release boundary remains under GAP-005."
  }
}
